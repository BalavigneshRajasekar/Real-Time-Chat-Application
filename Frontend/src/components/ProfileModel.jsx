/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import React from "react";

function ProfileModel({ show }) {
  return <>{show && <h1>Profile</h1>}</>;
}

export default ProfileModel;
